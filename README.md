# SEN Tutor

Static website for Eva's specialist SEN tutoring service.

Open `index.html` directly, or serve the repository root with any static file
server. GitHub Pages publishes the same root entry point without a build step.

## Tests

```powershell
python -m unittest discover -v
```
